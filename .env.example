# SafeHarbor Environment Configuration
# Copy this file to .env and customize the values

# =============================================================================
# Server Configuration
# =============================================================================
PORT=3000
NODE_ENV=production

# =============================================================================
# Security
# =============================================================================
# IMPORTANT: Change these to secure random values in production!
# Generate a secure JWT secret with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-random-secret-key

# Admin credentials - change these immediately after first login!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# =============================================================================
# Storage Paths
# =============================================================================
# For production deployments on Raspberry Pi or Linux servers:
DATA_DIR=/var/safeharbor/data
CONTENT_DIR=/var/safeharbor/content
ZIM_DIR=/var/safeharbor/zim
DATABASE_PATH=/var/safeharbor/safeharbor.db

# For development on local machine (uncomment these and comment out above):
# DATA_DIR=./data
# CONTENT_DIR=./content
# ZIM_DIR=./zim
# DATABASE_PATH=./safeharbor.db

# =============================================================================
# Kiwix Configuration
# =============================================================================
KIWIX_SERVE_PORT=8080

# Path to kiwix-serve binary (optional)
# Leave empty to use the bundled version in bin/kiwix-serve
# Or set to system binary if installed via apt: /usr/bin/kiwix-serve
# KIWIX_SERVE_PATH=

# =============================================================================
# Network Configuration (Raspberry Pi Hotspot Mode)
# =============================================================================
HOTSPOT_SSID=SafeHarbor
HOTSPOT_PASSWORD=safeharbor2024
NETWORK_INTERFACE=wlan0
